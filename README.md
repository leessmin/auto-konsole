# auto-konsole
适用于KDE Plasma6.5及其以上版本的konsole主题自适应

# 安装
```bash
yay -S auto-konsole
```

# 使用
```bash
systemctl --user enable --now auto-konsole.service

auto-konsole --light <主题名字>

auto-konsole --dark <主题名字>

auto-konsole --list #列出主题列表
```